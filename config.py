# Network & Backend Configuration
# Wi-Fi credentials are configured from a phone and stored on the device.
# Press B during boot to change networks.

# Legacy fallback only: the server URL saved through phone setup takes priority.
BACKEND_URL = ""

# Synchronization interval in milliseconds (e.g. 60000 = 60s)
SYNC_INTERVAL_MS = 60000

# Set False to disable timed light sleep when debugging via USB/Thonny.
LIGHT_SLEEP_ENABLED = True

# Temporary diagnostic screen after harvest; A/B dismisses it.
HARVEST_TIMING_SCREEN = True

# Enable only for serial debugging; keep False for battery/light-sleep tests.
SERIAL_LOG_ENABLED = False
